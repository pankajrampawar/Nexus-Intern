import Sidebar from '@/app/company/ui'


export default function CompanyLayout({ children }) {
    return (
        <main>
            <section>
                <Navbar/>
            </section>
        </main>
    )
}